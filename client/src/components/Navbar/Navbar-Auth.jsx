import React from 'react'

export const NavbarAuth = () => {
    return (
        <nav>
            <div class="nav-wrapper navbar blue">
                <a href="#" class="brand-logo center">Jwt Auth</a>
            </div>
        </nav>
    )
}

export default NavbarAuth
